services:
  - type: web
    name: church-deck
    env: docker
    plan: free
    healthCheckPath: /healthz
    envVars:
      - key: ESV_API_KEY
        sync: false
      - key: SHEET_CSV_URL
        sync: false
