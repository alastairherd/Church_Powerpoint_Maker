# Historical Python generator

These files preserve the previous local Python workflow for reference. They are excluded from active builds, CI, Docker images, and deployment. They are unsupported and may contain assumptions that no longer apply.

The Rust web application at the repository root is the only supported runtime. Do not restore the old Google Sheet, Hymnary scraping, hard-coded credential, or local Office automation paths to production code.
